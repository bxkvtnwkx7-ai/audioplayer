local name = peripheral.getName(peripheral.find("tape_drive"))

local f = fs.open("methods.txt", "w")

for _, method in ipairs(peripheral.getMethods(name)) do
    f.writeLine(method)
end

f.close()

print("saved")
